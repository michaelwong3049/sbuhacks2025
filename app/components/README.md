# Components Directory

## Structure
- `ui/` - Reusable UI components (buttons, cards, modals, etc.)
- `instruments/` - Instrument-specific components (DrumKit, Guitar, etc.)
- `calibration/` - Calibration UI components (zone setup, position guides)

## Team Member Assignment
**Frontend Developer** - Build all React components here

